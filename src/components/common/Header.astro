---
import { navigation } from '../../data/site-data'; import { withBase } from '../../utils/paths';
---
<header class="border-b border-black/10 bg-white"><div class="container flex min-h-20 items-center justify-between gap-6"><a class="font-bold no-underline" href={withBase('/')} aria-label="PI-ENSA, inicio">PI-ENSA <span class="sr-only">[Logotipo pendiente]</span></a><nav aria-label="NavegaciÃ³n principal"><details class="md:hidden"><summary class="cursor-pointer font-semibold">MenÃº</summary><ul class="absolute right-4 z-10 mt-3 rounded-xl border bg-white p-4 shadow-lg">{navigation.map(([label,href]) => <li><a class="block px-3 py-2" href={withBase(href)}>{label}</a></li>)}</ul></details><ul class="hidden items-center gap-4 md:flex">{navigation.map(([label,href]) => <li><a class="rounded px-2 py-2 no-underline hover:underline" href={withBase(href)}>{label}</a></li>)}</ul></nav></div></header>
